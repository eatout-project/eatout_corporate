export enum SelectedMenu {
  RESERVATIONS='reservations',
  MENUEDITOR='menuEditor',
  YOURMENU='yourMenu'
}

export enum ReservationStatus{
  WAITING = "waiting",
  APPROVED = "approved",
  DECLINED = "declined"
}
